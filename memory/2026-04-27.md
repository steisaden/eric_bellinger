# 2026-04-27 Memory Log

## Accomplishments
- Resolved carousel layout bug in `src/components/Interviews.tsx` where thumbnails were cut off on the left and shifted on the right. 
- Migrated the custom scroll container implementation to an explicit native layout using `snap-x`, `snap-mandatory`, and `overflow-x-auto` on the inner `flex` container, which provides stable viewport width calculations.
- Verified alignment via local browser tests at port 3001.
- Updated site document title in `index.html` to `"YFS It's Eazy"` per instruction.
- Conducted website audit for animation speed and load screens. Mitigated redundant favicon 404 triggers and expanded core control click-targets across modular sheets safely.
- Synced Footer branding attributes utilizing the shared morph-text configurations.
- Embedded full-spectrum verified external social routing protocols matching the designated platforms.
- Converted Featured Songs, Discography, and Interviews carousels into responsive 2x2 configurations optimized for touch swipe mechanics.

## Lessons Learned
- Relying on a parent wrapper's `overflow-hidden` combined with custom `offsetLeft` scrolls across unconstrained flexbox tracks introduces major cumulative coordinate errors if layout bounds shift. Standardizing carousels to use native snap scroll behaviors avoids this.
